import React from 'react';

class AboutPage extends React.Component {
  render () {
    return (
      <div>
        <h1>VoteUP</h1>
        <p>This application uses React, Redux, React Router for FRONTEND and NodeJs, ExpressJs, PassportJs, MongoDB, Mongoose & JWT Auth for BACKEND</p>
      </div>
    );
  }
}

export default AboutPage;
