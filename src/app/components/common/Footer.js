import React, { PropTypes } from 'react';

const Footer = (props) => {
  return (
    <footer className="footer">
      <p>Made with &hearts; by Asif Norzai</p>
    </footer>
  );
};

export default Footer;
